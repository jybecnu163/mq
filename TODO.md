#   1. 可靠性增强
    1.1 消息重试与死信队列
    现状：消费失败只能依赖客户端自行处理，无自动重试或死信机制。
    改进：服务端记录每条消息的投递次数，失败后自动重试 N 次（可配置），重试间隔指数退避。超过上限后转入死信主题 (DLQ-原topic)，便于人工排查或异步修复。这可以避免单条“坏消息”拖死消费者。
    
    1.2 生产者发送确认与幂等性
    现状：客户端同步等待 RESPONSE，但未提供消息级别的去重保证。
    改进：在消息中增加全局唯一的 messageId，Broker 侧缓存已处理的消息 ID（短期），对重复发送进行幂等去重。生产者可以安全地重试发送，而不必担心重复消息。
    
    1.3 Broker 优雅关闭与启动恢复
    现状：直接 kill 会可能丢失未刷盘的数据或造成文件损坏。
    改进：监听 JVM shutdown hook，停止接受新连接，清空队列中的写入，关闭文件通道，标记活跃段为已关闭。重启时快速校验分段文件的完整性，跳过损坏的末尾部分。
    
    1.4 消息持久化刷盘策略
    现状：依赖 FileChannel.write（可能只写到 OS 缓存）。
    改进：提供同步刷盘（force）选项，在关键业务中保证单条/批量消息落盘后再确认，降低断电丢失风险。

#   2. 可观测性增强
    2.1 完善 Prometheus 指标
    新增指标：mq_messages_consumed_total（按 topic、group）、mq_consumer_lag（已部分实现）、请求处理耗时分布 (Histogram)、mq_active_connections、磁盘写入速率、文件段数、延时消息积压量等。这些指标帮助快速定位瓶颈和异常。
    
    2.2 消息轨迹与审计
    改进：为每条消息记录生产时间、消费者组消费时间、ACK 时间，存储到专门的轨迹主题或文件中。提供按 messageId 或 topic 查询消息全生命周期的能力，方便问题排查。
    
    2.3 健康检查与就绪探测
    现状：仅有 metrics 端口，没有专用健康端点。
    改进：提供 /health（返回 UP）和 /ready（检查文件系统可写、内存时间轮正常等），用于 Kubernetes 探针或负载均衡器后端检测。
    
    2.4 集成分布式追踪
    改进：在消息中携带 traceId，通过 OpenTracing 集成（如 Jaeger），将消息的发送、存储、投递过程串联起来，实现端到端的调用链追踪。

#   3. 运维友好性
    3.1 配置外部化
    现状：端口、数据目录等硬编码。
    改进：统一使用配置文件（YAML/Properties）和环境变量覆盖，支持 --config 命令行参数。让部署时无需重新编译。
    
    3.2 管理控制台或 API 增强
    现状：已有 /admin/topics 等基础接口。
    改进：增加主题创建/删除、消费者组重置偏移量（按时间或位置）、分段文件清理触发、黑名单管理等运维接口。可内置一个简单的 Web UI。
    
    3.3 数据治理工具
    现状：分段文件需手动清理。
    改进：提供自动清理策略（按时间、按磁盘使用率），以及手动触发 compact（合并小段）。提供 mqadmin 命令行工具，方便运维人员操作。
    
    3.4 平滑扩容与数据迁移
    当前局限：单机系统无法扩容。
    后续方向：实现分区（Partition）后，提供分区重新分配工具，在新增或下线节点时自动迁移分区数据，客户端无感。
    
#   4. 客户端易用性
    4.1 批量消息发送
    改进：在 Producer 中增加批量 API，将多条消息打包成一个请求，提升网络利用率和 Broker 写入吞吐。

    4.2 异步发送与回调
    改进：提供非阻塞的发送方法，支持 SendCallback，不阻塞业务线程，充分利用异步 IO。
    
    4.3 消费者自动订阅发现
    改进：通过简单的主题通配符或正则表达式，自动订阅新创建的主题，无需重启客户端。
    
    4.4 连接池与负载均衡（集群化后）
    改进：当有多个 Broker 节点时，客户端能根据负载均衡策略（轮询、哈希）选择连接，单一节点故障自动切换到其他节点。
    
    4.5 序列化框架选择
    改进：允许用户自定义消息序列化/反序列化器，不仅限于 JSON，可支持 Protobuf、自定义二进制等，提高性能和灵活性。

#   5. 其他特色功能
    5.1 消息过滤增强
    现状：支持基于单 Tag 的简单匹配。
    改进：支持更丰富的表达式（如 tag1 && tag2、tag1 || tag2），甚至 SQL92 条件过滤（基于消息属性）。
    
    5.2 延时消息优先级与取消
    现状：延时消息一旦提交无法取消或修改。
    改进：支持取消或修改延时消息（通过 requestId），支持优先级调度（高优先级消息提前投递）。
    
    5.3 安全认证与传输加密
    改进：添加基于 Token 或用户密码的简单认证，支持 TLS/SSL 加密通信，保护生产环境安全。
    
    5.4 集成测试工具
    改进：提供嵌入式 Broker 测试依赖，方便业务单元测试中直接启动内存级 Broker，避免依赖外部环境。