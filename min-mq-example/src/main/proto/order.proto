syntax = "proto3";

// 如果希望生成的 Java 类在 com.mymq.example.proto 包下
package com.mymq.example.proto;

// 订单消息
message Order {
  int64 order_id = 1;
  string user_name = 2;
  double amount = 3;
  string status = 4;
}